# 简历快投助手浏览器扩展 - 当前检查报告

## 文件检查

- `manifest.json` 使用 Manifest V3。
- 工具栏点击不再打开 popup，改为直接唤起网页右侧浮动面板。
- 主界面文件为 `content/content.js` 和 `content/content.css`。
- 设置页文件为 `options/options.html`、`options/options.js` 和 `options/options.css`。
- 图标已使用 `icons/logo-16.png`、`icons/logo-32.png`、`icons/logo-48.png`、`icons/logo-128.png`。

## 功能状态

- 已保留：分类管理、字段管理、文本输入框一键粘贴、字数计数、超过 500 字提示。
- 已移除：Word / JSON / PDF 简历解析、解析预览、解析导入分类逻辑。
- 已移除：JSON 备份导入导出，避免和普通简历 JSON 混淆。
- 已移除：浮动面板顶部无用折叠箭头。
- 已支持：Chrome 以及多数 Chromium 内核浏览器的本地开发者模式安装。

## 本轮验证

- `options/options.js` 语法检查通过。
- `content/content.js` 语法检查通过。
- `background/background.js` 语法检查通过。
- `manifest.json` 引用的脚本、样式、图标路径检查通过。
